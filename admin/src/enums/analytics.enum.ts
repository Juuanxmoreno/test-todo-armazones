export enum AnalyticsPeriod {
  Today = "today",
  ThisWeek = "thisWeek",
  ThisMonth = "thisMonth",
  ThisYear = "thisYear",
  Custom = "custom",
  AllTime = "allTime",
}

export enum AnalyticsGranularity {
  Hour = "hour",
  Day = "day",
  Week = "week",
  Month = "month",
}

export enum AnalyticsTimeZone {
  Argentina = "America/Argentina/Buenos_Aires",
}

export enum AnalyticsMetricType {
  Orders = "orders",
  Users = "users",
  Stock = "stock",
}
